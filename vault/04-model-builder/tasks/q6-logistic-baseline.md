# Task: Q6 Logistic Regression Baseline

## Steps
1. Load X_train, y_train, X_test, y_test from data/features/
2. Train LogisticRegression(max_iter=1000, random_state=42)
3. Predict on test set (both classes and probabilities)
4. Compute: accuracy, precision, recall, F1, AUC-ROC, AUC-PR
5. Plot ROC curve, confusion matrix
6. Write metric justification for public policy context
7. Save model to results/models/logistic_baseline.joblib
